import { mingster } from "@/app/blog/authors";

export const meta = {
  title: "CentOS: Install git",
  description: "1. Download and install EPEL if you don't have it yet:rpm -Uvh http://download.fedora.redhat.com/pub/epel/5/i386/epel-release-5-3.noarch.rpm 2. Install git via...",
  date: "2010-04-03T00:00:00.000Z",
  authors: [mingster],
};

<p>1. Download and install EPEL if you don't have it yet:<br /><code>rpm -Uvh http://download.fedora.redhat.com/pub/epel/5/i386/epel-release-5-3.noarch.rpm<br /></code></p>
<p>2. Install git via yum<br /><code>yum --enablerepo=epel install git</code></p>
<p></p>
