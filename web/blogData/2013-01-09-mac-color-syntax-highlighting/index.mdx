import { mingster } from "@/app/blog/authors";

export const meta = {
  title: "mac: color syntax highlighting",
  description: "To get the same syntax highlight as in bash shall, add this to ~/.profile export CLICOLOR=cons25 In vi, add the lines below in ~/.vimrc, create the file if you...",
  date: "2013-01-09T00:00:00.000Z",
  authors: [mingster],
};

<p>To get the same syntax highlight as in bash shall, add this to ~/.profile<br />
<code>export CLICOLOR=cons25</code></p>
<p>In vi, add the lines below in ~/.vimrc, create the file if you don't have it yet:<br />
<code><br />
:set term=builtin_ansi<br />
:syntax on<br />
</code></p>
